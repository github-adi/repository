-My build of DIGITAL Command Language:
-Muhammad Adi Nugroho,S.Adm.Neg.:
-Via My Notepad++ (HTML Editor):
file:///C:/Users/Axioo/node_modules/.bin/json5:
#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/C:/Users/Axioo/node_modules/json5/lib/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/C:/Users/Axioo/node_modules/json5/lib/cli.js" "$@"
  ret=$?
fi
exit $ret


