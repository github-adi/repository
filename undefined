-If the code scripts can not run at Notepad HTML Editor, execute it at right mouse-Inspect Element-Elements-Console-Enter or Toolbar right-Settings-More Tools-Developer Tools.

var x;
if (x === void 0) {
   // these statements execute
}

// y has not been declared before
if (y === void 0) {
   // throws a ReferenceError (in contrast to `typeof`)
}
-
Returned Error can not be declared because y not defined. If y to be changed with -1, returned undefined.
